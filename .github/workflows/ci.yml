# CI disabled for this fork — build and deployment are handled by docker-build.yml
# Tests from upstream require unreleased dependency versions incompatible with this environment.
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI skipped for VSM fork — see docker-build.yml for build status"
